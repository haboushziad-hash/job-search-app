# Secrets — never commit
.env
.env.local
.env.*.local
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Tauri / Rust
target/
desktop_app/src-tauri/target/
*.rs.bk

# Build artifacts
dist/
build/
out/
.next/
.astro/

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Local archive (large, lives only on your PC)
archive/runs/*.json
archive/scrapes/*.json
archive/scores/*.json
archive/raw_jds/*.txt
!archive/.gitkeep

# Tester audit dumps + local run outputs (contain personal data)
audit_handoff/
scripts/full_search_output/
scripts/test_runs/
backend/storage/runs/

# Test/stress/health outputs (may contain tester data, not needed in repo)
scripts/keyword_test_output/
scripts/stress_test_output/
scripts/health_log/
archive/validation_results/

# PyInstaller-built backend binary — produced by CI per platform, never committed.
# This file is 100+ MB and exceeds GitHub's per-file size limit anyway.
desktop_app/src-tauri/binaries/

# Orphan debug HTML
icims_snapon_iframe.html
icims_snapon_inner.html
landing/http_server.log
landing/http_server.log.err

# SQLite — local-only state
*.db
*.db-wal
*.db-shm

# Built installer artifacts (live in R2, not git — they're 100+ MB)
*.msi
*.dmg
*.app/

# Personal documents (resumes, cover letters, etc. — PII)
*.pdf
*.docx
*.doc
!docs/**/*.pdf

# PyInstaller build outputs
backend/build/
backend/dist/

# Cloudflare wrangler state
.wrangler/
.dev.vars

# Test outputs
test_results/
*.log

# Profile-builder fixture cache (auto-invalidating, regenerated on demand)
tests/profile_fixtures/.cache/

# Working artifact for cross-Claude-chat handoff (not source)
Copy and Paste/
_geocoding_additions.txt
cf_worker/tester_audit.json
