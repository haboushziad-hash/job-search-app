# v0.1.4 State — Pre-Compression Snapshot

**Date:** 2026-05-04
**Purpose:** Capture every commit, decision, and remaining task so we can pick up cleanly after compression.

---

## TL;DR — Where We Are

**v0.1.3 + v0.1.4 are nearly tag-ready.** All major engineering work
is committed. Currently waiting on Ryan's cross-persona proxy run
(~10 min from now) to complete Phase 16. After that: revert
binaries/.env, version bump, user GUI smoke test, tag.

---

## Full Commit History (most recent first)

```
b19a340 v0.1.4 landing: add JSearch + Arbeitnow to scraper marquee
ed2b6ff v0.1.4: per-source quota detection in audit JSON
20ebc14 v0.1.4 fix: spread STRETCH-tier scores across 25-49 range
1ed2617 v0.1.4 jsearch most-aggressive: route Tier 1/2 keywords to JSearch only
847011b v0.1.4 fix: Anthropic 400 — auto-pad max_tokens for thinking budget
a870949 v0.1.4 jsearch: num_pages 1 -> 2 (post-Pro-upgrade)
e2cb37e v0.1.4 jsearch: num_pages 2 -> 3 + FULLTIME filter (Pro tier optimized)
93cd3cf v0.1.4 hot-fix: ScraperClient sends X-Tester-UUID header in proxy mode
408bfa4 v0.1.4 phases 13+14: Worker proxies for keyed scrapers + JSearch
f7ea42b v0.1.4 phase 15: persisted overrides + pre-flight budget check + cap bump
91db83b v0.1.4 phase 12: search-terms vs target-titles split
8181d7b v0.1.3 hot-fix: Workday CXS JD fetch (unlocks 10-20 qualifying roles)
4ef32e6 v0.1.3 hot-fix: unicode arrow -> ASCII in scraper orchestrator prints
3d7933e v0.1.3 phase 10: KV write sampling (98% reduction in PUTs)
5a19236 v0.1.3 phase 9: version bump + test runner + morning runbook
db83827 v0.1.3 phase 8: speed wins (S1-S4)
1e9df20 v0.1.3 phase 6: audit JSON serializer fixes
dfa0533 v0.1.3 phase 5: UI/UX fixes
fee6d57 v0.1.3 phase 4: tenant health + per-tenant backoff
ea50ac4 v0.1.3 phase 3: scraper matcher unification
662e17f v0.1.3 phase 2: hard filter fixes
42416e7 v0.1.3 phase 1: scoring/cascade fixes
```

---

## Active Background Task

**Ryan proxy run (`bqiim3x57`)** — kicked off, in profile-build phase as of last check. Expected ~10 min total. When it completes:
- Output saved to `scripts/test_runs/ryan_proxy_full_<ts>.json`
- Audit JSON in `%USERPROFILE%\Documents\JobSearchApp\audits\runs\`
- Monitor `bzx3bwxdy` will fire `RYAN_PROXY_DONE`

Compare Ryan's STRONG count against the dev-mode baseline of 9. Other Claude predicted: "If Ryan's STRONG+GOOD jumps from 19 to 25+, you have the coverage story validated across all personas."

---

## Remaining Tasks Before Tag

| # | Item | Effort | Status |
|---|---|---|---|
| 1 | Wait for Ryan's run to complete + audit | ~10 min wait + 5 min audit | 🟡 In progress |
| 2 | Revert `binaries/.env` LLM_PROXY_URL to empty | 5 sec | ⏳ Pending |
| 3 | Bump version 0.1.3 → 0.1.4 in `tauri.conf.json` + `package.json` | 30 sec | ⏳ Pending |
| 4 | Final commit "v0.1.4 ship" | 30 sec | ⏳ Pending |
| 5 | User: rebuild backend.exe + restart Tauri dev + run a real GUI search | ~15-30 min | ⏳ Pending — user's call |
| 6 | `git tag v0.1.4 && git push --tags` | 30 sec | ⏳ Pending |
| 7 | Watch GHA build | 10-15 min | ⏳ Pending |

---

## Test Run Context

### Ziad — confirmed working through proxy mode

- Run ID: `27f7c6df-034c-4757-b0a2-5f8a0853100a` (last verification)
- File: `scripts/test_runs/ziad_proxy_full2_20260504_121505.json`
- Duration: 9.2 min, Cost: $0.78
- Total qualifying: **168**
- STRONG: 30, GOOD: 30, MAYBE: 25, STRETCH: 83
- All 4 scraper proxies returned 200 OK (Adzuna, USAJOBS, Findwork, JSearch)
- LLM proxy: 1006 Gemini calls all through Worker
- Top STRONGs included CliftonLarsonAllen, PwC Assurance, World Bank Group (JSearch finds)

### Zach — confirmed working through proxy mode

- Run ID: `60f1aea1-e834-49ec-9943-b095d72a1dda`
- File: `scripts/test_runs/zach_proxy_full_20260504_124037.json`
- Duration: 9.8 min, Cost: $0.73
- Total qualifying: **137**
- STRONG: 18 (DOUBLED from dev-mode 9), GOOD: 26, MAYBE: 22, STRETCH: 71
- Top STRONGs included Nulo Pet Food, Penske rotational program (validates the early-career rotational guidance Phase 12 added)

### Ryan — 🟡 in progress
Will be the third leg of the cross-persona validation.

---

## Verified-Working Architecture

| Layer | Verification |
|---|---|
| Worker LLM Gemini proxy | ✅ 1006 calls, no 429s |
| Worker LLM Anthropic proxy | ✅ Routes correctly (model 400 was pre-existing, fixed in commit 847011b) |
| Worker Adzuna proxy | ✅ HTTP 200 |
| Worker USAJOBS proxy | ✅ HTTP 200 |
| Worker Findwork proxy | ✅ HTTP 200 |
| Worker JSearch proxy | ✅ HTTP 200 (10K/month Pro tier purchased) |
| Worker `/v1/llm/budget` pre-flight check | ✅ Returns sampled counter state |
| KV write sampling (1-in-50) | ✅ ~7 PUTs/search vs prior 330 |
| DAILY_CALL_CAP = 5000 per UUID | ✅ Worker version `e6012268` deployed |
| ScraperClient sends X-Tester-UUID header | ✅ Commit `93cd3cf` |
| Search-terms vs target-titles split | ✅ Commit `91db83b` |
| JSearch num_pages=3 + FULLTIME + Tier 1+2 keywords | ✅ Commits `e2cb37e` + `1ed2617` |
| Per-source quota detection in audit | ✅ Commit `ed2b6ff` |
| Anthropic 400 fix (auto-pad max_tokens > thinking budget) | ✅ Commit `847011b` (verified via curl 400→200) |
| Score-42 STRETCH spread fix in Stage 2 prompt | ✅ Commit `20ebc14` |
| Workday CXS JD fetch (Phase 13 hot-fix) | ✅ Commit `8181d7b` (Workday went 0 → 36 qualifying) |
| Continue-in-background fix (global poll hook) | ✅ Commit `dfa0533` (verified in code) |
| Token-overlap matcher unification (15 scrapers) | ✅ Commit `ea50ac4` (matcher coverage went 1% → 100%) |
| Stage 3 schema fix | ✅ v0.1.3 phase 1 (was 100% broken, now works) |

---

## Current `.env` Files State

**Project-root `.env`** (`C:\Users\habou\OneDrive\Desktop\Job Search App\.env`):
- `LLM_PROXY_URL=` (empty — reverted to dev mode)
- All API keys present (GOOGLE_API_KEY×3, ANTHROPIC_API_KEY, ADZUNA_*, USAJOBS_*, FINDWORK_*, etc.)

**`binaries/.env`** (`...desktop_app\src-tauri\binaries\.env`):
- `LLM_PROXY_URL=https://api.findmesomedamnjobz.com` ⚠️ **NEEDS REVERT TO EMPTY**
- `TESTER_UUID=72e2aa7f-34a3-45ee-8cb2-cdee07722765` ⚠️ **NEEDS REVERT TO EMPTY**
- This is the file the bundled backend.exe reads at runtime
- For ship: must be empty so production build doesn't ship hardcoded proxy URL
- Tester builds will get LLM_PROXY_URL injected via Tauri sidecar config (separate path)

**Worker secrets** (`wrangler secret list`):
- ✅ ADMIN_TOKEN
- ✅ ADZUNA_APP_ID + ADZUNA_APP_KEY
- ✅ ANTHROPIC_API_KEY
- ✅ FINDWORK_API_KEY
- ✅ GOOGLE_API_KEY (×3)
- ✅ JSEARCH_RAPIDAPI_KEY (Pro tier purchased)
- ✅ USAJOBS_API_KEY + USAJOBS_USER_AGENT

---

## Worker Deployment State

- **Latest version:** `e6012268-0718-4c78-a9ae-d08a872c6ec7`
- Endpoints live:
  - `POST /v1/llm/gemini/...` (deep proxy)
  - `POST /v1/llm/anthropic/...` (deep proxy)
  - `GET /v1/llm/budget` (pre-flight)
  - `* /v1/scraper/{adzuna,usajobs,findwork,jsearch}/...` (4 keyed-scraper proxies)
  - `POST /v1/runs` (audit upload)
  - `GET /v1/runs/me` (per-tester history)
  - `GET /v1/update/latest.json` (auto-update manifest)
  - `GET /v1/update/download/{platform}` (signed bundle serving)
- KV cap counter sampled at 1:50, daily cap 5000 per UUID

---

## Open Decisions / Design Choices Locked

| Decision | Choice | Rationale |
|---|---|---|
| Tag name | `v0.1.4` | Continuing the train. Significant scope but not breaking. |
| JSearch tier | Pro $25/mo (purchased) | 200/month free was 14 searches; pilot needs ~30+/month |
| Adzuna tier | Free (1K/month) for now | Email sent asking for free-tier extension. Will hit limit ~Day 8-10; JSearch picks up slack via legitimate aggregation overlap |
| LinkedIn / Indeed / Glassdoor scrapers | NOT shipping | ToS violation; JSearch covers them legitimately |
| Continue-in-background | Verified in code, not yet GUI-tested | User's smoke test step will verify |
| iCIMS investigation | Deferred to v0.2 | Likely Chromium-bundling issue, packaging infra problem |
| Workday tenant list | Trimmed 41 → 27 | Drops retail/pharma/heavy industry low-AI-density tenants |

---

## Known Issues Carried Forward to v0.1.5

| # | Issue | Severity | Plan |
|---|---|---|---|
| 1 | iCIMS scraper silent (0/0 across runs) | Medium | Likely Chromium not bundled in PyInstaller. Option: replace with HTTP-only iCIMS scraper or move Playwright to Worker side. |
| 2 | Workday: low qualifying despite high raw (4500 → 14) | Medium | Workday's keyword search is too broad; matcher already culls. Per-source funnel reveals the loss point. v0.1.5 investigation. |
| 3 | Adzuna conversion drops with broader search_terms | Low | API treats multi-word as adjacent. Either accept the drop or send Adzuna a different keyword vector. |
| 4 | No final liveness re-check immediately before output | Low | Currently 2-minute racy window between Stage 3 and dashboard. Acceptable but could add. |

---

## v0.1.4 Files Changed (full list)

### Backend
- `backend/api.py` — `/llm/budget` proxy endpoint, search_terms preference for /search/run
- `backend/config.py` — (unchanged structurally; just env var reads)
- `backend/models.py` — `CandidateProfile.search_terms: list[str]`
- `backend/profile/builder.py` — search_terms generation in prompt + parsing + `keywords_to_search_strings`
- `backend/runner.py` — search_terms support, JSearch keyword expansion, post-Stage-3 salary re-filter, Writer regional dedup, S3 speed win
- `backend/filter/hard_filters.py` — UK title leak fix, salary band 15→10%
- `backend/scraper/_keyword_match.py` (NEW) — shared token-overlap matcher
- `backend/scraper/base.py` — `quota_exhausted` attribute
- `backend/scraper/orchestrator.py` — `extra_jsearch_keywords` param, post-fetch sanity filter, quota surfacing
- `backend/scraper/adzuna.py` — proxy-aware routing, quota detection
- `backend/scraper/usajobs.py` — proxy-aware routing, quota detection
- `backend/scraper/findwork.py` — proxy-aware routing, quota detection
- `backend/scraper/jsearch.py` (NEW) — JSearch scraper + proxy-aware routing
- `backend/scraper/greenhouse.py` — refactored to use `_keyword_match`
- `backend/scraper/lever.py` — token-overlap matcher
- `backend/scraper/ashby.py` — token-overlap matcher + 404 prune
- `backend/scraper/arbeitnow.py` — token-overlap matcher
- `backend/scraper/hn_hiring.py` — token-overlap matcher
- `backend/scraper/smartrecruiters.py` — token-overlap matcher
- `backend/scraper/workday.py` — tenant trim 41→27, 30s timeout, 500-error backoff, CXS JD fetch
- `backend/scraper/builtin.py` — graceful 403 handling
- `backend/scraper/client.py` — X-Tester-UUID default header in proxy mode
- `backend/scoring/stage2_triage.py` — tier labels removed, title-headline floor (PRINCIPLE 8), JD truncation 8K→16K, STRETCH spread fix
- `backend/scoring/stage3_deep_eval.py` — tier labels removed, JD truncation 12K→16K, schema fix, skip_above 88→101, second-look 0.7→0.8
- `backend/scoring/orchestrator.py` — concurrency 8/6, skip_above 88→101
- `backend/scoring/anthropic_client.py` — auto-pad max_tokens for thinking budget
- `backend/storage/audit.py` — wire per_source_funnel + cost_by_stage + excluded_title_patterns

### Worker
- `cf_worker/src/index.ts` — DAILY_CALL_CAP 250→2000→5000, KV sampling 1:50, `/v1/llm/budget`, 4 scraper proxy endpoints, audit upload, manifest serve

### Frontend (Tauri React)
- `desktop_app/src/App.tsx` — `useActiveSearchPoll()` global hook
- `desktop_app/src/hooks/useActiveSearchPoll.ts` (NEW) — global polling
- `desktop_app/src/components/Sidebar.tsx` — running indicator
- `desktop_app/src/pages/Running.tsx` — refactored to consume global state
- `desktop_app/src/pages/Setup.tsx` — pre-populate from prior profile (salary, locations, arrangements)
- `desktop_app/src/pages/Keywords.tsx` — search_terms UI section + budget pre-flight check
- `desktop_app/src/services/api.ts` — `getBudget()` client
- `desktop_app/src/types/index.ts` — `search_terms`, `acceptable_location_radii` fields

### Landing
- `landing/src/pages/index.astro` — JSearch + Arbeitnow added to marquee

### Docs / Scripts
- `docs/v0.1.3_RUNBOOK.md`
- `docs/v0.1.3_VERIFICATION_FRAMEWORK.md`
- `docs/TESTER_INTRO.md` (NEW)
- `docs/v0.1.4_state_pre_compression.md` (THIS FILE)
- `scripts/test_run_search.py` (NEW) — drives `/profile/build` + `/search/run` directly
- `scripts/diagnose_workday_embedding.py` (NEW) — diagnostic for Workday CXS fix
- `scripts/start_backend_proxy.bat` (NEW) — proxy-mode launcher

---

## How to Resume After Compression

1. Read `docs/v0.1.4_state_pre_compression.md` (this file)
2. Check Ryan's run status:
   - `tail "C:/Users/habou/AppData/Local/Temp/claude/.../bqiim3x57.output"`
   - Or: `ls -t "C:/Users/habou/Documents/JobSearchApp/audits/runs/"*.json | head -1`
3. If Ryan's audit JSON exists: pull `STRONG/GOOD/total qualifying` from it; compare to dev-mode baseline of 19 STRONG+GOOD
4. Revert `desktop_app/src-tauri/binaries/.env`: change `LLM_PROXY_URL=...` to `LLM_PROXY_URL=` and `TESTER_UUID=...` to `TESTER_UUID=`
5. Bump version 0.1.3 → 0.1.4 in `desktop_app/src-tauri/tauri.conf.json` and `desktop_app/package.json`
6. Final commit: `v0.1.4: ship — verified end-to-end across 3 personas`
7. Hand off to user for GUI smoke test
8. After user signoff: `git tag v0.1.4 && git push --tags`

---

## Costs Confirmed

Per-search cost (proxy mode, full v0.1.4):
- Ziad: $0.78 / 9.2 min (168 qualifying)
- Zach: $0.73 / 9.8 min (137 qualifying)
- Ryan: TBD

Monthly tester pilot (4 testers × 2 searches/week × 4 weeks = 32 searches):
- Gemini API: ~$25-30/mo
- Anthropic API: ~$2-5/mo (cross-model audit, now working)
- JSearch Pro: $25/mo
- Adzuna: free until ~Day 10, then needs paid tier (TBD pricing)
- Cloudflare Worker: free tier (KV sampling fix keeps us well under)
- R2 storage: ~$0-1/mo
- **Estimated total: $55-65/mo for 4-tester pilot**

---

## Polish Items NOT Done (acceptable for v0.1.4 ship)

- Continue-in-background live GUI test (verified in code, not GUI)
- Run-to-run diff visualization in dashboard
- Real-time cost tracking in Running page
- In-app audit JSON viewer
- Score history per role across runs
- Compare-runs UI

These are v0.1.5+ polish, not ship blockers.
