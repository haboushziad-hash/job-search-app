# Job Search App — Backend dependencies

# Core
python-dotenv==1.0.1            # Load .env files
pydantic==2.9.2                 # Schema validation for LLM outputs
pyyaml==6.0.2                   # Config files

# LLM providers
google-genai>=1.74.0            # Gemini API (latest official SDK)
anthropic>=0.40.0               # Claude API (used for cross-model consensus)
httpx>=0.28.1                   # Async HTTP for Cloudflare AI + others (>=0.28.1 required by google-genai)
tenacity==9.0.0                 # Retry with exponential backoff

# Data
# v0.3.2: pinned to <2 because NumPy 2.x macOS wheels link against
# Apple's Accelerate framework using the new $NEWLAPACK$ILP64 ABI,
# which is broken/missing across multiple macOS releases (verified on
# both old and new Macs in the field — testers hit dlopen symbol-not-
# found errors that crashed the bundled backend on launch).
# 1.26.4 is the last 1.x release; uses the old, universally-stable
# LAPACK API and works across every macOS we care about.
# Tied to python<3.13 in CI because 1.26 has no official 3.13 wheels.
numpy==1.26.4                   # Embedding similarity math

# Scraping
beautifulsoup4==4.12.3
lxml==5.3.0

# Resume parsing
pypdf==5.1.0
python-docx==1.1.2

# API server (bridge to React)
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Utilities
rich==13.9.2                    # Pretty CLI output for dev/debug
tqdm==4.66.5                    # Progress bars

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
